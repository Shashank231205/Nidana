# Eval reports

Committed, dated, never overwritten. One file per run.

A `clinical:` or `prompt:` commit carries the path to the report for its run in
the commit body. A prompt change with no eval run does not merge.

Empty until the vignette set exists. Running the harness against zero cases
produces a report saying zero cases, which is not evidence of anything.
